import React from 'react'
import styles from './index.module.scss'

type Props = {}

const Newsletter: React.FC<Props> = (props) => {
  return <div className={styles.newsletter}>index</div>
}

export default Newsletter
